# Журнал работ (русский)

**Создан:** 2026-02-08  
**Назначение:** краткая запись нашей работы в простом языке. Сюда вносим по вашей команде («запиши», «зафиксируй» и т.п.).

---

## Записи

### Запись 1 — 2026-02-08 (первая запись дня)

**Что сделали:**

1. **Правила для Cursor** — создали папку `.cursor/rules/` и файлы правил: общие правила проекта (SOLID, KISS, русский язык), архитектура AaC/C4, правила схем (только Mermaid, именование, Schema First), работа с руководствами (спрашивать разрешение, ждать подтверждения). Потом все правила собрали в `docs/project-rules.md`, в том числе: «что умеешь» — только рассказывать, не делать; схемы показывать в чате в Markdown; внешний браузер не открывать.

2. **Структура проекта** — создали папки: `docs/architecture/adr`, `common`, `glossary` и домены в `domains/` (участки, взносы, показания счётчиков, банковские выписки, уведомления, отчётность).

3. **Руководство по синхронизации с репозиторием** — файл `docs/repository-sync-guide.md`: где вводить команды (терминал), как делать pull/push, правило «агент спрашивает разрешение и ждёт подтверждения».

4. **Архитектура и диаграммы:**
   - Библиотека компонентов в `docs/architecture/common/`: акторы, внешние системы, БД (файлы `.mmd`).
   - Схема L1 (контекст системы) — КОНТРОЛЛИНГ, собственники, председатель, казначей, банк, Telegram, почта, SMS.
   - Глоссарий домена «взносы» в `docs/architecture/glossary/contributions.md`.
   - Шаблон ADR в `docs/architecture/adr/ADR-template.md`.
   - Схема L2 (контейнеры) для домена «банковские выписки» в `domains/bank_statements/container-diagram.mmd`.

5. **Git и репозиторий (вариант B — всё в текущем проекте):** создали `.gitignore` (Python и артефакты), `README.md` с описанием КОНТРОЛЛИНГ и структуры. Закоммитили и отправили в GitHub (ветка `main`). Репозиторий уже был привязан.

6. **Ошибка рендеринга Mermaid на GitHub** — в файлах `.mmd` были лишние строки с обратными кавычками; рендеру нужен только чистый код. Исправили: во всех `.mmd` оставили только код диаграммы. Добавили `docs/architecture/diagrams.md` с блоками Mermaid для корректного отображения на GitHub. Снова закоммитили и отправили.

7. **Папка логов** — создали папку `logs/` и два файла-заготовки журнала с датой в имени: русский (`work-log_2026-02-08_ru.md`) и английский (`work-log_2026-02-08_en.md`) — для записей по вашей команде.

### Запись 2 — 2026-02-08 (конец дня)

**Что сделали:**

8. **Логи и README на GitHub** — закоммитили и отправили папку `logs/`; в корневой `README.md` добавили в структуру пункт про `/logs/` (журналы работ).

9. **Расширение Mermaid в Cursor** — установили `bierner.markdown-mermaid` для красивого отображения диаграмм в превью Markdown.

10. **Дубликат для Obsidian** — папка `obsidian/`: файл «КОНТРОЛЛИНГ — диаграммы.md» с теми же Mermaid-диаграммами и `README.md` с инструкцией, как открыть в Obsidian.

11. **Правила: вопрос ≠ действие** — в `docs/project-rules.md` и `.cursor/rules/guide-workflow.mdc` усилено правило: если пользователь **спрашивает** («можно ли», «can we» и т.п.) — только отвечать, ничего не делать. Выполнять только при явной просьбе («сделай», «выполни», «подтверждаю»).

12. **Синхронизация с Git** — все изменения закоммичены и отправлены в `origin main`. Работа на сегодня завершена.

---

*Папка `logs/` используется для журналов по архитектуре и другим задачам.*
