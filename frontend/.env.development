# В dev API идёт через прокси Vite (/api → бэкенд), baseURL задаётся в api.ts как /api/v1.
# Для продакшена задайте VITE_API_BASE_URL (полный URL бэкенда).
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
