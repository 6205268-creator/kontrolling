name: Backend tests

on:
  push:
    branches: [main, master]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-tests.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-tests.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    # Backend tests use in-memory SQLite: conftest.py sets DATABASE_URL before importing app.
    # No PostgreSQL service or DATABASE_URL secret required for this job.
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        # DATABASE_URL set by conftest.py to sqlite+aiosqlite:///:memory: before app import; no PostgreSQL needed
        run: pytest -q

      - name: Install ruff
        run: pip install ruff

      - name: Ruff check
        run: ruff check .
        continue-on-error: false

      - name: Ruff format (check)
        run: ruff format --check .
