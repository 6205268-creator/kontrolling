---
## Выполнено

**Версия:** 1  
**Дата:** 2026-02-24  

**Простыми словами:**  
Настроен автоматический запуск тестов и линтеров при каждом push в репозиторий: отдельные GitHub Actions для бэкенда (pytest, ruff) и фронтенда (ESLint, Vitest). Во фронтенд добавлены ESLint, Prettier и Vitest со скриптами `lint` и `test`. В README добавлены бейджи статуса сборки.

**Роль в структуре проекта (технически):**  
CI/CD слой: `.github/workflows/` — два workflow (backend-tests.yml, frontend-tests.yml), триггеры по push/PR с фильтром путей. Backend: Python 3.11, pip, pytest, ruff check/format. Frontend: Node 20, pnpm, eslint (flat config), vitest (vite test). Линтеры и тесты фронтенда живут в `frontend/` (eslint.config.js, .prettierrc, vite.config.ts test block, package.json scripts). Опционально: backend/.pre-commit-config.yaml для локального ruff. От фичи зависят: стабильные сборки при PR, бейджи в README; дальше — Фича 35 (E2E).
---

# Фича 34: CI/CD — GitHub Actions (тесты и линтеры)

**Цель:** Workflows для backend и frontend, ruff/eslint.

## Контекст

- `docs/project-implementation.md` — секция «Фича 34»
- Создать: `.github/workflows/backend-tests.yml`, `.github/workflows/frontend-tests.yml`
- Опционально: `backend/.pre-commit-config.yaml`
- `backend/`, `frontend/` (структура для pytest и pnpm test/lint)

## Правила

- `.cursor/rules/project-conventions.mdc`

## Зависимости

- F01, F20 (тесты backend и frontend)
