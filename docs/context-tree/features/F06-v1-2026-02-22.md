---
## Выполнено

**Версия:** 1
**Дата:** 2026-02-22

**Простыми словами:**
Добавлен учёт расходов товарищества: справочник категорий (дороги, зарплата, материалы) и сами расходы с суммой, датой, документом и статусом. Расходы не привязаны к участкам или взносам — это отдельный финансовый поток (казна СТ как юрлица).

**Роль в структуре проекта (технически):**
Модели `ExpenseCategory` и `Expense` в `backend/app/models/`. Expense имеет `cooperative_id`, `category_id`, `amount`, `expense_date`, `document_number`, `status` (created → confirmed → cancelled). Связь: Cooperative → expenses. Миграция `0005_add_expense.py`. Тесты в `test_models/test_expense.py` (5 тестов). Основа для API расходов (фича 17) и отчётов по расходам СТ.
---

# Фича 6: Модели данных — Expense (расходы СТ)

**Цель:** Модели Expense, ExpenseCategory.

## Контекст

- `docs/project-implementation.md` — секция «Фича 6»
- `docs/data-model/entities-minimal.md` — Expense (если есть)
- `backend/app/models/cooperative.py`, `backend/app/models/__init__.py`
- Создать: `backend/app/models/expense_category.py`, `backend/app/models/expense.py`
- Создать: `backend/alembic/versions/0005_*.py`
- Создать: `backend/tests/test_models/test_expense.py`

## Правила

- `.cursor/rules/project-conventions.mdc`
- `.cursor/rules/architecture-rules.mdc`

## Зависимости

- F01, F02 (Cooperative)
