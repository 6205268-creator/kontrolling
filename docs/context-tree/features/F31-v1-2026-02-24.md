---
## Выполнено

**Версия:** 1  
**Дата:** 2026-02-24  

**Простыми словами:**  
Добавлен скрипт, который одним запуском заполняет базу тестовыми данными (два СТ, владельцы, участки, начисления, платежи, расходы, счётчики, пользователи). Для тестов добавлены общие фикстуры — один раз описанные «образцы» сущностей (кооператив, владелец, участок и т.д.), которые тесты могут подключать без копирования кода.

**Роль в структуре проекта (технически):**  
Слой данных и тестов: `backend/scripts/seed_db.py` — точка входа для наполнения БД (запуск из каталога backend: `python -m app.scripts.seed_db`). `backend/tests/fixtures.py` — pytest-фикстуры (sample_cooperative, sample_owner, sample_land_plot, sample_plot_ownership, sample_contribution_type, sample_expense_category, sample_financial_subject, sample_accrual, sample_payment, sample_expense, sample_meter, sample_meter_reading, sample_app_user), подключаются через `pytest_plugins = ["tests.fixtures"]` в conftest.py. От фич 31 не тянется код в продакшен-логику; зависимость — от моделей F02–F08.

---

# Фича 31: Seed данные и тестовые фикстуры

**Цель:** Скрипт seed_db.py, pytest fixtures.

## Контекст

- `docs/project-implementation.md` — секция «Фича 31»
- Все модели backend (cooperative, owner, land_plot, plot_ownership, financial_subject, accrual, payment, expense, meter, app_user)
- Создать: `backend/scripts/seed_db.py`, `backend/tests/fixtures.py`

## Правила

- `.cursor/rules/project-conventions.mdc`

## Зависимости

- F02–F08 (все модели)
