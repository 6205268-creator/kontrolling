# Архив v0 (Python/FastAPI)

Старая документация проекта на стеке Python + FastAPI + PostgreSQL + Vue 3.

Принято решение перейти на **1С + OData + Vue 3**.

**Причина:** бизнес-логика уже реализована в 1С, нет смысла дублировать backend на Python. OData из коробки покрывает потребности фронтенда.
